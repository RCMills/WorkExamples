CREATE TABLE [dbo].[Designation] (
    [DesignationId]          INT            NOT NULL,
    [DesignationDescription] NVARCHAR (100) NOT NULL,
    CONSTRAINT [PK__Designation_Id] PRIMARY KEY CLUSTERED ([DesignationId] ASC)
);

